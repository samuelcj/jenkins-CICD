def gv

pipeline {
    agent any
    stages {
        stage("init") {
            steps {
                script {
                    gv = load "./java-maven-app/script.groovy"
                }
            }
        }
        stage("build") {
            steps {
                script {
                    gv.buildApp()
                }
            }
        }
        stage("test") {
            input {
                message "Select the evironment for the deployment"
                ok "Done"
                parameters {
                    choice(name: "Environment", choices: ["dev", "prod"], description: "Selecting Environment")
                }
            }
            steps {
                script {
                    echo "Deploying to ${Environment}"
                    gv.testApp()
                }
            }
        }
        stage("deploy") {
            steps {
                script {
                    env.ENV = nput message: "Select the evironment for the deployment", ok: "Done", parameters: [choice(name: "ENV", choices: ["dev", "prod"], description: "Selecting Environment")]
                    gv.deployApp()
                    echo "The Environment has been set to ${ENV}"
                }
            }
        }
    }   
}